set $mod Mod1
set $sup Mod4

font pango:JetBrainsMono Nerd Font Mono 10

exec --no-startup-id dex --autostart --environment i3

exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock -c 1a1b26 --nofork

exec --no-startup-id nm-applet

floating_modifier $mod

tiling_drag modifier titlebar

bindsym $mod+Return exec --no-startup-id ghostty

bindsym $mod+Shift+q kill

# Start Brave
bindsym $mod+Shift+f exec --no-startup-id brave

# Take Screenshot
bindsym Print exec --no-startup-id maim "/home/$USER/Pictures/$(date)"
bindsym $mod+Print exec --no-startup-id maim --window $(xdotool getactivewindow) "/home/$USER/Pictures/$(date)"
bindsym $mod+Shift+Print exec --no-startup-id maim --select "/home/$USER/Pictures/$(date)"

# Clipboard Screenshot
# bindsym Ctrl+Shift+S exec --no-startup-id maim | xclip -selection clipboard -t image/png
bindsym Ctrl+Shift+s exec --no-startup-id maim --select | xclip -selection clipboard -t image/png

# start dmenu (a program launcher)
# bindsym $mod+d exec --no-startup-id dmenu_run
bindsym $mod+space exec --no-startup-id .config/rofi.vaibhav/files/launchers/type-1/launcher.sh

# alternatively, you can use the cursor keys:
bindsym $mod+l focus right
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up

# move focused window
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right
bindsym $mod+Shift+h move left

# split in vertical orientation
# bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# Moving to prev and next workspaces
bindsym Ctrl+$mod+Right workspace next
bindsym Ctrl+$mod+Left workspace prev

# Moving between monitors
bindsym $mod+Ctrl+greater move workspace to output right
bindsym $mod+Ctrl+less move workspace to output left

# Setup for Clipboard Manager
bindsym $mod+Shift+B exec copyq


# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
# bindsym $mod+Shift+ focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

set $i3lockwall i3lock -c 1a1b26

bindsym $sup+l exec --no-startup-id $i3lockwall

# focus the child container
# bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $term "1"
set $browser "2"
set $code "3"
set $postman "4"
set $slack "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $term
bindsym $mod+2 workspace number $browser
bindsym $mod+3 workspace number $code
bindsym $mod+4 workspace number $postman
bindsym $mod+5 workspace number $slack
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8


bindsym $mod+Shift+1 move container to workspace number $term
bindsym $mod+Shift+2 move container to workspace number $browser
bindsym $mod+Shift+3 move container to workspace number $code
bindsym $mod+Shift+4 move container to workspace number $postman
bindsym $mod+Shift+5 move container to workspace number $slack
bindsym $mod+Shift+6 move container to workspace number $ws6

bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

# Volume controls
set $volumestep 5
set $statuscmd i3status
set $volumepath /home/vaibhav/i3-volume/volume 
set $statussig SIGUSR1

# Volume contols using i3-volume and playerctl
bindsym XF86AudioRaiseVolume exec --no-startup-id $volumepath -n -t $statuscmd -u $statussig up $volumestep
bindsym XF86AudioLowerVolume exec --no-startup-id $volumepath -n -t $statuscmd -u $statussig down $volumestep
bindsym XF86AudioMute        exec --no-startup-id $volumepath -n -t $statuscmd -u $statussig mute
bindsym XF86AudioPlay exec playerctl play-pause
bindsym XF86AudioPause exec playerctl pause
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous 

bindsym Ctrl+Right exec --no-startup-id $volumepath -n -t $statuscmd -u $statussig up $volumestep
bindsym Ctrl+Left exec --no-startup-id $volumepath -n -t $statuscmd -u $statussig down $volumestep
bindsym Ctrl+Down exec --no-startup-id $volumepath -n -t $statuscmd -u $statussig mute

# Resize mode
bindsym $mod+r mode "resize"

# class                 border  backgr. text    indicator child_border
client.focused          #000000 #000000 #ffffff #121212   #121212
client.focused_inactive #333333 #5f676a #ffffff #484e50   #5f676a
client.unfocused        #333333 #222222 #888888 #292d2e   #222222
client.urgent           #2f343a #900000 #ffffff #900000   #900000
client.placeholder      #000000 #0c0c0c #ffffff #000000   #0c0c0c

focus_follows_mouse no
popup_during_fullscreen smart

# shutdown / restart / suspend...
set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (CTRL+s) shutdown

mode "$mode_system" {
    bindsym l exec --no-startup-id $i3lockwall, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id $i3lockwall && systemctl suspend, mode "default"
    bindsym h exec --no-startup-id $i3lockwall && systemctl hibernate, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Ctrl+s exec --no-startup-id systemctl poweroff -i, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+BackSpace mode "$mode_system"
# end here

# Display mode
set $mode_display Laptop (l), Desktop (d), Exit (Escape)
set $display_script $HOME/.config/i3/scripts/change-display.sh
mode "$mode_display" {
    bindsym l exec --no-startup-id $display_script laptop, mode "default"
    bindsym d exec --no-startup-id $display_script desktop, mode "default"

    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bindsym $sup+$mod+d mode "$mode_display"
# end

# Navigator Assignments
assign [class="Alacritty" instance="Alacritty"] $term
assign [class="ghostty" instance="ghostty"] $term
assign [class="firefox" instance="Navigator"] $browser
assign [class="Brave-browser" instance="brave-browser"] $browser
assign [class="Code" instance="code"] $code
assign [class="Postman" instance="postman"] $postman
assign [class="MongoDB Compass" instance="mongodb compass"] $postman
assign [class="Slack" instance="slack"] $slack

for_window [window_role="alert"] floating enable
no_focus [window_role="alert"]

# Gaps and borders config
smart_gaps on
smart_borders on
hide_edge_borders smart_no_gaps
gaps inner 8

exec --no-startup-id feh --randomize --bg-fill /home/$USER/.config/i3/assets/wallpapers/

exec xsets s off
exec xset -dpms
exec "xautolock -detectsleep -time 25 -locker \"i3lock -c 1a1b26\""
exec_always --no-startup-id $HOME/.config/polybar/launch.sh
exec_always picom --config /home/$USER/.config/picom/picom.conf

exec --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

exec --no-startup-id /usr/lib/gsd-xsettings
exec --no-startup-id /usr/lib/gnome-settings-daemon
